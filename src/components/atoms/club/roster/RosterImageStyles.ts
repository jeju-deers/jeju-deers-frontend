import tw from "twin.macro";
import styled from "styled-components";

export const RosterPersonImage = styled.img`
  ${tw`
    bg-white
  `}
`;

export const RosterImageBox = styled.div`
  ${tw`
  `}
`;
