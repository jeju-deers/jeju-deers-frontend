import tw from "twin.macro";
import styled from "styled-components";

export const PlayerPersonImage = styled.img`
  ${tw`
    bg-white
  `}
`;

export const PlayerImageBox = styled.div`
  ${tw`
  `}
`;
