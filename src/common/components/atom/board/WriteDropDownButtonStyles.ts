import tw from "twin.macro";
import styled from "styled-components";

export const WriteDropdownButtonSelect = styled.select`
  ${tw`
    border
    border-gray-300
    h-11
    w-full
    p-2
    rounded-md
  `}
`;
